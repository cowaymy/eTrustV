<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coway.trust.biz.payment.payment.service.impl.RentalCollectionByBSMapper">

    <!-- RentalCollectionByBS 리스트 조회 -->
	<select id="searchRentalCollectionByBSList" parameterType="rentalCollectionByBSSearchVO" resultType="rentalCollectionByBSListVO">
	   SELECT 
	       S_ID AS sID,
		   T_ORG_CODE AS tOrgCode, 
		   T_GRP_CODE AS tGrpCode, 
		   T_DEPT_CODE AS tDeptCode, 
		   ORG_CODE AS orgCode, 
		   GRP_CODE AS grpCode, 
		   DEPT_CODE AS deptCode, 
		   MEM_CODE AS memCode, 
		   S_UNIT AS sUnit, 
		   S_LMOS AS sLmos, 
		   S_CM_CHG AS sCmChg, 
		   S_CL_CTG AS sClCtg, 
		   S_COL AS sCol, 
		   S_ADJ AS sAdj, 
		   S_OUT AS sOut, 
		   S_OUT_RATE AS sOutRate
        FROM REP0014S
        WHERE
        1=1
        <if test="orgCode != '' ">
           AND T_ORG_CODE = #{orgCode} 
       </if>
       <if test="grpCode != '' ">
           AND T_GRP_CODE = #{grpCode} 
       </if>
       <if test="deptCode != '' ">
           AND T_DEPT_CODE = #{deptCode} 
       </if>
       <if test="memCode != '' ">
           AND MEM_CODE = #{memCode} 
       </if>
	</select>
</mapper>